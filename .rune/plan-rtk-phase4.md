# Phase 4: Metrics + UI

## Goal
Dashboard showing token savings, cost impact, per-strategy breakdown.

## Tasks
- [ ] RTK metrics store — per-session savings, per-strategy counts, historical data
- [ ] Dashboard widget — "RTK saved X tokens ($Y) this session"
- [ ] Per-message savings indicator in chat UI
- [ ] Settings panel — enable/disable strategies, adjust thresholds
- [ ] Savings report endpoint — GET /api/sessions/:id/rtk-stats

## Dependencies
- Phase 1-3 complete (or at least Phase 1 for basic metrics)
