# Companion — Agent Instructions

> Auto-generated by XLabs Scanner. Do NOT edit manually — changes will be overwritten.
> Last updated: 2026-03-16T07:27:50.049Z

## You Are

An XLabs autonomous agent working on **Companion** (`companion`).
You are part of a fleet of AI agents managed by XLabs orchestrator.
Your work is tracked, reviewed, and scored automatically.

## XLabs Tools (MCP)

XLabs MCP server is available at `http://localhost:3461`. Use these tools to interact with the organization.

### Before Writing Code

ALWAYS call these tools first to understand context:

1. `xlabs_get_project_context("companion")` — tech stack, conventions, danger zones, health score
2. `xlabs_get_relevant_research("companion")` — research findings that apply to this project
3. `xlabs_get_open_incidents("companion")` — known issues to avoid
4. `xlabs_get_previous_feedback("companion")` — learn from past agent sessions
5. `xlabs_check_budget()` — verify budget before expensive operations

### During Execution

6. `xlabs_report_progress(task_id, percentage, status_text)` — update dashboard with progress
7. `xlabs_log_decision(type, subject, chosen, reasoning, confidence)` — log significant decisions

### After Completing Work

8. `xlabs_create_task(project_id, title, description, priority)` — create follow-up tasks
9. `xlabs_update_deploy_status(project_slug, channel, status)` — update deploy status after deploying

### Fleet Awareness

10. `xlabs_get_fleet_overview()` — see all projects health, active tasks
11. `xlabs_get_task_history("companion")` — recent completed tasks with summaries

## Tech Stack

- TBD — will split from MyTrend

## Danger Zones

No danger zones defined yet.

## Deploy

```bash
TBD
```

## Current Health

- Health Score: 0/100
- Files: 0 | Lines: 0
- Tests: NO — add tests!
- CI/CD: NO
- Linting: NO

## Known Issues

- Low health score: 0/100
- No tests directory found
- No CI/CD configuration
- No commits in last 7 days

## Notes

Not started yet — empty repo, will be extracted from MyTrend

## Agent Rules

1. **Always check XLabs context first** — call `xlabs_get_project_context` before writing code
2. **Report progress** — call `xlabs_report_progress` at 25%, 50%, 75%, 100%
3. **Log decisions** — any non-trivial architectural choice should be logged
4. **Create follow-ups** — if you discover issues outside your task scope, create a new task
5. **Respect danger zones** — extra testing required for flagged paths
6. **No hardcoded secrets** — use environment variables
7. **Commit with semantic messages** — `feat:`, `fix:`, `refactor:`, etc.
8. **Run verification** after every code change
