# Companion — Agent Instructions

> Auto-generated by XLabs Scanner. Do NOT edit manually — changes will be overwritten.
> Last updated: 2026-04-18T06:28:22.072Z

## You Are

An XLabs autonomous agent working on **Companion** (`companion`).
You are part of a fleet of AI agents managed by XLabs orchestrator.
Your work is tracked, reviewed, and scored automatically.

## XLabs Tools (MCP)

XLabs MCP server: `https://admin.theio.vn/api/mcp`. Full portal: `https://admin.theio.vn/docs/agent`.

### Unified Session Flow (MANDATORY)

All work follows this 6-step sequence:

1. `xlabs_start_session({ slug: "companion", task_id })` — returns composite brief (context + incidents + feedback + research + recent tasks). **Call FIRST.**
2. `xlabs_log_decision(type, subject, chosen, reasoning, confidence)` — log your plan before executing
3. Execute. Call `xlabs_report_progress(task_id, percentage, status_text)` at 25%, 50%, 75%
4. `xlabs_submit_feedback(project_slug, type, content, severity)` — report blockers, suggestions, bugs found
5. `xlabs_create_task("companion", title, description, priority)` — create follow-ups for out-of-scope work
6. `xlabs_end_session({ project_slug: "companion", summary, success })` — close session with summary. **Call LAST.**

### Individual Tools (when needed outside the flow)

- `xlabs_get_project_context("companion")` — re-fetch project context mid-session
- `xlabs_check_budget()` — verify budget before expensive operations
- `xlabs_update_deploy_status(project_slug, channel, status)` — update deploy status after deploying
- `xlabs_suggest_config_improvement(...)` — when you discover config that should change
- `xlabs_escalate(task_id, question, severity)` — pause for owner decision (when judgment is needed)
- `xlabs_get_fleet_overview()` — cross-project health view

## Tech Stack

- TBD — will split from MyTrend

## Danger Zones

No danger zones defined yet.

## Deploy

```bash
TBD
```

## Current Health

- Health Score: 65/100
- Files: 1048 | Lines: 2,059,002
- Tests: NO — add tests!
- CI/CD: NO
- Linting: Yes

## Code Structure

- Entities: 2521 functions, 255 interfaces, 153 variables, 69 types, 55 exports, 41 classs
- Key clusters: packages/server (1567), packages/web (1388), packages/shared (127), scripts (9), landing (3)

## Known Issues

- No tests directory found
- No CI/CD configuration
- Working directory has uncommitted changes

## Notes

Not started yet — empty repo, will be extracted from MyTrend

## Agent Rules

1. **Start with `xlabs_start_session`** — the unified brief replaces 5 separate calls
2. **Report progress** at 25%, 50%, 75% via `xlabs_report_progress`
3. **Log decisions** — any non-trivial architectural choice should be logged
4. **Submit feedback** — end-of-session blockers/suggestions go via `xlabs_submit_feedback`
5. **Create follow-ups** — out-of-scope work becomes a new task
6. **Respect danger zones** — extra testing required for flagged paths
7. **No hardcoded secrets** — use environment variables
8. **Commit with semantic messages** — `feat:`, `fix:`, `refactor:`, etc.
9. **Run verification** after every code change
10. **Close with `xlabs_end_session`** — records outcome for Director learning
